net-create-2.sh will create the network resources.
net-destroy-2.sh will tear down the network resources.
config-lbaas-ha-dvr will config the lbaas to be accessable from external network.
config-ha-dvr-ext will config the vm to access external network through 2 routers.
router-jump.txt some notes when debug.